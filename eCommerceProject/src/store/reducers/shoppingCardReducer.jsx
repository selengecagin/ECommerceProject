const initialState = {
  cart: [],
  payment: {},
  address: {},
};

const shoppingCardReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default shoppingCardReducer;
